extends ./layout.pug

block title
  title #{__('Account Register')}

block scripts
  link(rel='stylesheet', type='text/css', href='stylesheets/sign-in-modules.css')
  script(type='text/javascript' src='https://code.jquery.com/jquery-2.2.4.min.js')
  script(type='text/javascript' src='scripts/mask-plugin.js')

block body
  form.pure-form.pure-form-stacked(action='/registerProcessForm', method='post')
    fieldset
      .upper-box
        div
          h1.title-padding #{__('Register an account')}
          if error
            .subtext #{error}
        .pure-control-group
          label.labels(for='name') #{__('Name')}:
          .input-fields
            input#name.pure-input-1.input-fields(type='text', placeholder=__('Name'), name='name')
        .pure-control-group
          label.labels(for='mobileNumber') #{__('Mobile Number')}:
          .input-fields
            input#mobileNumber.pure-input-1.input-fields(type='tel', placeholder=__('Mobile Number'), pattern='+7([0-9]{3})[0-9]{3}-[0-9]{2}-[0-9]{2}', name='mobileNumber', value=mobileNumber)
      .bottom-box
        .pure-controls.sign-in-button
          button.pure-button.pure-button-primary(type='submit', value=__('Submit')) #{__('Register')}
